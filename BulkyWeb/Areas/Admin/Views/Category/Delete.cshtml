@model Category

@{
    ViewData["Title"] = "Delete";
}
<fieldset class="border m-4">
    <h1 class="p-2">Delete Category</h1>
    <div>
        <h3 class="p-2 text-danger">Are you sure you want to delete this?</h3>
        <dl class="row p-2">
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.Name)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.Name)
            </dd>
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.DisplayOrder)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.DisplayOrder)
            </dd>
        </dl>

        <form method="post" asp-controller="Category" asp-action="Delete" class="p-2">
            @* <input type="hidden" asp-for="Id" /> *@
            @Html.HiddenFor(model => model.Id)
            <input type="submit" value="Delete" class="btn btn-danger" />
            <a asp-action="Index" class="btn btn-outline-light">Return to List</a>
        </form>
    </div>

</fieldset>
